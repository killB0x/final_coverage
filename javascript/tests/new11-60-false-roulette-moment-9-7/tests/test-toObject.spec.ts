export {}
import {toObject} from "../../../syntest-javascript/benchmark/top10npm/moment/src/lib/moment/to-type.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('toObject', () => {
	it('test for toObject', async () => {
		const _returnValuekIYYCEn = await toObject()
	});
})